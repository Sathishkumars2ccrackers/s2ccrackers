rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Customer authentication and client-side database collections have been removed.
    // Storefront operates directly via the secure REST API.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
